 package student.bin_tree;

import student.node.Node;

class BinTree<E>{
  private Node<E> root;

  public BinTree(E element) {
    this.root = new Node<>(element);
  }

  public BinTree(){
    this.root = null;
  }
  public String preOrder(){
    String p= "";
    //root izquierda derecha
    if (root!=null){
      String p = 
      return 
    }
    else{
      return
    }
  }
  p

  
}